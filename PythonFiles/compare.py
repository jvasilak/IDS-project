import numpy as np
from numpy.linalg import norm
# a and b are both numpy arrays, this function should be used to compute one factor, not the combination
cosine_similarity(a, b):
    return (np.dot(a, b)/())
